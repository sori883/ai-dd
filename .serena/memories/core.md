# Project Core
- Repository: `sori883/ai-dd`; default branch `main`.
- Current project is an initial scaffold: tracked `README.md`; no `go.mod` or `src/` implementation yet.
- Durable engineering rules live in root `AGENTS.md`.
- Project Codex configuration: `.codex/config.toml`; project custom agents: `.codex/agents/*.toml`; reusable skills: `.agents/skills/*/SKILL.md`.
- Human-only `work/` is out of scope unless explicitly requested.
- Language/toolchain facts: `mem:tech_stack`.
- Repository conventions: `mem:conventions`.
- Useful commands: `mem:suggested_commands`.
- Completion gates: `mem:task_completion`.