# Tech Stack
- Implementation language is Go by repository policy.
- Prefer the Go standard library; adding an external Go module requires rationale plus explicit user approval.
- No Go module or application source exists yet.
- Codex project configuration uses TOML under `.codex/`; project skills use Markdown `SKILL.md` under `.agents/skills/`.
- MCP servers configured in `.codex/config.toml`: Context7 and Serena.