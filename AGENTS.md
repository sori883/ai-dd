## 開発ルール
- 不明点は想像せず、ユーザーへ質問して確認してください。
- 実装作業に着手する前に、実装計画をユーザーへ提示し、レビューと明示的な承認を得てください。承認されるまで、実装コードを作成または変更しないでください。
- Goで実装してください。
- Go標準ライブラリを優先してください。外部Go moduleを追加する場合は、必要性と設計理由を説明し、ユーザーの明示的な承認を得てください。

## 本家AI-DLCとの意図的な差分提示

- 本家と異なる仕様・挙動を設計判断として意図的に採用する場合だけ、追加の指示を待たず、実装前の計画と完了報告で「本家の挙動」「採用する挙動」「変更理由」「利用者・互換性への影響」を提示してください。
- Goへの移植や通常の実装過程で結果として生じる差分（言語・型・内部構造など）、段階的実装の未実装部分は、この差分提示ルールの対象外です。それらを都度列挙したり、差分があることだけを理由に追加承認を求めたりする必要はありません。
- 意図的な差分を提示する際は、参照した本家のバージョンと根拠、確認範囲を明記してください。ローカル参照スナップショットを最新upstreamと同一視せず、未確認の箇所を「差分なし」と断定しないでください。
- 承認済み計画にない意図的な仕様・挙動の変更を採用する場合は、実装前にユーザーへ提示して明示承認を得てください。合意した変更と理由は `docs/ram/` に記録し、索引も更新してください。
- この限定は差分提示ルールにのみ適用します。実装計画の承認、テスト、バグ・契約違反の確認は従来どおり行ってください。

## 配置ルール
- 設計、マイルストーン、準拠方針、開発・運用手順などのドキュメントは、プロジェクトルートの `docs/` 配下に格納してください。
- 開発対象のドキュメント、ソースコードは`src/`配下に格納してください。
- Codex custom agent、skill、MCP設定はCodexのdiscovery pathを優先し、それぞれ `.codex/`、`.agents/skills/` に格納してください。これらの設定ファイルは `src/` ルールの例外です。

## プロジェクトRAM運用

- 開発プロジェクトの永続的な意思決定、合意した制約、採用・却下した選択肢、調査結果、未解決事項は `docs/ram/` に記録してください。
- ユーザーへ選択や確認を求め、その回答が今後の設計、実装、配布、運用に影響する場合は、原則として同じタスク内で回答と理由をRAMへ記録してください。
- 計画、設計、調査、実装を始める前やユーザーへ既出事項を質問する前に、`docs/ram/README.md` と関連記録を確認してください。
- 記録を追加または更新した場合は、`docs/ram/README.md` の索引も同じタスク内で更新してください。
- 決定を変更する場合は過去の記録を削除または上書きせず、新しい記録から置換対象を参照してください。
- `docs/ram/` は本リポジトリを開発する側の記憶です。AI-DLCが利用プロジェクト内で扱う `aidlc/spaces/<space>/knowledge/` やDocumentKBとは混在させないでください。
- 一時的な作業メモ、秘密情報、利用プロジェクト固有の成果物知識はRAMへ保存しないでください。記録対象か不明な場合はユーザーへ確認してください。

## タスクルール

- 実装を行う時はGitHubにIssueを作成してください。
- 実装をマージする時はPRを作成してください、
- PRはIssueと紐づけてください。
- PRマージ後及び、作業完了後Issueを閉じてください。

## GitHub Issue・PR運用

- Issue・PRの作成、更新、参照、または現在の実装状況を確認するときは、
  `github-pr-workflow` skillを使用してください。
- Issue・PRのタイトル、本文、コメント、レビュー要約とユーザーへの報告は、コード識別子、
  コマンド、パス、既存タイトルの原文などを除き、日本語を基本としてください。
- 現在の実装状況は、default branchへマージ済みのPRを第一の履歴根拠として確認してください。
  Draft、Open、未マージのClosed PRを実装済みとせず、後続PR、revert、現在のコードとテストも
  必要な範囲で照合してください。
- PR本文には、対応Issue、実装した機能、検証、レビューを記載し、後から実装範囲を確認できる
  ようにしてください。通常の未実装機能や「今回は作らないもの」は列挙せず、特殊な利用条件・
  互換性・安全性の制約がある場合だけ特記事項として記載してください。本家AI-DLCとの差分は、
  上記ルールに従い意図的な仕様・挙動の変更だけを記載してください。
- 自動マージを設定しないでください。mergeはユーザーが明示的に依頼した場合だけ行ってください。

## MCP 利用ルール
- 外部ライブラリや API の仕様、設定手順、コード例を確認するときは、Context7 を優先して参照してください。
- Codex セッションの開始時に、Serena で現在のリポジトリをプロジェクトとして有効化し、Serena の初期指示を確認してください。
- MCP が利用できない場合は、組み込みツールで作業を継続し、利用できなかった MCP と理由をユーザーへ伝えてください。

## カスタムエージェント運用

- 複雑な変更では、`project_planner`、`technical_researcher`、`go_tdd_implementer`、`independent_reviewer` を役割に応じて使用してください。
- 計画と技術調査は読み取り専用で実行し、必要な場合のみ並列化してください。
- 実装担当は常に1エージェントとし、同じ作業ツリーを複数エージェントで同時編集しないでください。
- Goの作業では最初に `golang-how-to` skillを読み、タスクに必要なGo skillsを追加で使用してください。
- 親エージェントが承認、GitHub Issue、実装、独立レビュー、PRの各ゲートを管理してください。サブエージェントは、明示的に委譲されない限りIssueやPRを操作しません。
- 詳細なhandoff契約と停止条件は `docs/agent-workflow.md` に従ってください。

## Code Review Rules

- 承認記録と必要性の説明がない外部Go moduleの追加を指摘してください。安全な経路は、標準ライブラリで代替するか、追加前にユーザーの明示承認を得ることです。
- 観測可能な動作を変更する差分に、その変更前の実装では失敗する回帰テストがない場合は指摘してください。
- 承認済み計画または紐づくIssueの範囲を超える実装を指摘し、別タスクへ分離する安全な経路を示してください。
- 本家AI-DLCから意図的に変更した仕様・挙動について、必要な提示・承認・RAM記録がない場合は指摘してください。通常の実装過程で生じた差分だけを理由に、この提示ルールへの違反とは扱わないでください。

## その他

- `work/`は人間の作業用です。特別な指示がない限りアクセス不要です。
