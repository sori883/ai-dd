# エージェントの起動はメインAIの標準ツールで行う

状態: ユーザー指定の制約を確認。A/B案の選択・製品実装の承認ではない。2026-09-10。

## ユーザーの指定と現在の責任分担

ユーザーは「A/B案どちらがいい？CLI経由でエージェントを起動したくないです」と述べ、続けて「今はCLI経由でエージェントを起動してないですよね」と確認した。

現在はメインAIがCodexの標準サブエージェントツールで担当を起動する。AI-DLC CLIは手順取得、進捗・承認・Unit割当等を扱い、worker起動を代行しない。根拠は `src/harness/codex/minimal/SKILL.md:9`、`:14` と `src/harness/codex/minimal/aidlc-cli/SKILL.md:33`。

今後の案でも、この起動責任を維持する。AI-DLC CLIからCodexや子エージェントを起動する専用実行経路は採用しない。この指定から、CLIによる作業場所の登録・確認まで禁止されたとは解釈しない。

## A/B案への影響

[既存のA/B比較](2026-09-10-upstream-aligned-worker-control-options.md)のA案も、CLIは作業を登録し、メインAIが子を直接起動する方式である。A案を推奨する理由は、起動責任を維持しつつ、正規CLIによる作業場所の二重割当を防げる設計に絞れるためである。

B案は実workerの稼働制限を目指すが、子と予定rootの対応、停止・再開、hook故障時の未確定事項が残る。専用起動CLIへ切り替える代案は今回の指定に適合しない。標準起動ツールとhookだけで元の保証が成立するとは断定しない。

ユーザーはA/Bの選択をまだ回答していない。A案への推奨と、元の要求から二重割当防止へ保証対象を変えることへの承認を混同しない。製品コード・設定・Issue・PRは変更していない。
