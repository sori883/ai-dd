# 実案件、配布・更新、利用文書を順に進める

状態: 直接依頼。2026-09-10に残対応の説明に対し、ユーザーは
「じゃまず一番から順次対応してもらえますか」と回答した。

依頼された順序は、①最新版の製品構成で実案件1件を完走、②配布・更新の仕上げ、③利用者文書の更新。
旧33 Stageロードマップの承認を流用せず、この依頼を作業開始の根拠とする。
各作業では具体的な計画、Issue、単独writer、独立review、final、PR/checksの手順を維持する。
公開API・永続data・互換性・安全性・運用の重大な未決事項は個別に確認する。

①の対象は以前候補になっていた `intent check --help` の文書検査案内を最新6段階へ具体化する。
対象選択は小さな改善で実案件を完走する目的の範囲内と判断した。新しいSensor判定条件は追加しない。
実施管理は [Issue #163](https://github.com/sori883/ai-dd/issues/163)。
実装・実行の具体契約は [最新構成の実案件計画](../../design/latest-workflow-pilot-plan.md) に記録する。
以前の [check help案](../../design/check-help-pilot-plan.md) は検討履歴として保持する。

製品が要求するステージごとの成果承認は今回の開始依頼で代用しない。
初期化を含め、実成果と独立reviewを提示した後のユーザー回答を記録する。
配布する製品agentと本リポジトリの開発agentを区別し、自動hookを観測していない操作を実機成功に数えない。
初回のhook信頼確認とassignment管理開始の人間確認も維持する。

②と③は従来の保留記録を順次再開する依頼として扱う。
既存Intentの自動移行、既存ファイルの削除、CLIによるagent起動、外部Go module追加は含まない。
