# READMEの見出しをAI-DDにする

2026-09-13、ユーザーはREADME冒頭の`# AI-DLC`を`# AI-DD`へ変更し、Issueは作成しないよう指定した。

直接の修正依頼に基づき、READMEの見出しだけを変更する。CLI名やコマンド、本文の操作手順は変更しない。本記録とRAM索引へ判断を残し、差分と`git diff --check`で確認する。今回の修正ではIssueを作成しない。
