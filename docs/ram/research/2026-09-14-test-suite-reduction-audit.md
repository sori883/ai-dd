# テストの削除・統合候補を監査する

2026年9月14日。ユーザーはコードベース内のテストを精査し、実装のコピー、モックの自己検証、標準ライブラリ保証の再確認、重複、内部実装への依存、過剰なケース分けを積極的に削減する候補一覧を依頼した。

判断基準は「削るとどんな現実的な不具合を見逃すか」。件数・カバレッジ維持を目的にせず、実行時間・不安定さ・修正負担と保証の価値を比較する。今回の許可は調査と一覧作成であり、テスト・製品コード・CIの変更、Issue/PR作成、公開の実施許可として扱わない。

対象はPR #203マージ後のmain、commit `adc4682ca265c9bda4434a94d9d99cff1f4b9394`。`/Users/const/sori883/ai-dd-release` に調査branch `codex/test-suite-audit` を作成した。元の `/Users/const/sori883/ai-dd` の未commit変更は触らない。

[詳細一覧](../../test-suite-reduction-audit.md)へ全候補・対象位置・残す試験・失う保証・コスト・確度と確認範囲をまとめた。srcの216 test file、35,850行とCI2 workflow、別枠の外部skill説明用testを読んだ。新しいテスト実行・Codex実機実験は行っていない。既存の同tree final-04ログはコストと検証範囲の参考とし、今回の候補を削除した後の成功証拠とは扱わない。

主要な所見は、別名によるTest再呼出、廃止入口・不足したStepID等による前段拒否、同じ資材の固定hash/文章断片の多重検査、不要なGit・ビルド・解凍の反復。競合、途中保存、承認、managed assignment、集合SHAの対象範囲、実FS安全性は固有の不具合を防ぐため、重複部分だけを集約する。

独立照合で、GitHandoffの40桁HEADはruntime欠落を期待する負例であることを確認した。これを形式不一致だけで無効なtestとは判断しない。現行stateのGit共有も旧形式互換とは区別し、Git演習を縮小する場合もruntime非共有・無管理run拒否を保持する。配布の固定hashを外す場合は独立した必須資材・role/権限・license期待値と配置bytes/Paths/mode検査を残す。

削減の実装・CIの頻度変更は未実施。詳細一覧の条件付き候補は選択肢と保証の差を明記したもので、自動削除の決定ではない。
