interface:
  display_name: "Implementation Planning"
  short_description: "Create authorization-ready repository implementation plans"
  default_prompt: "Use $implementation-planning to produce an authorization-ready plan and identify its approval source."
policy:
  allow_implicit_invocation: true
