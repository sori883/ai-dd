interface:
  display_name: "Implementation Planning"
  short_description: "Create approval-ready repository implementation plans"
  default_prompt: "Use $implementation-planning to produce an approval-ready plan for this change."
policy:
  allow_implicit_invocation: true
