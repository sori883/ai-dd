interface:
  display_name: "Go Code Review"
  short_description: "Review Go diffs for correctness, risk, and test gaps"
  default_prompt: "Use $go-code-review to independently review this Go change against its authorized plan and Issue."
policy:
  allow_implicit_invocation: true
